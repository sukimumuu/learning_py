def titleline(req_length, title):
    t_lines = print("=" * req_length + f" {title} " + "=" * req_length)
    return t_lines

def eqlines(req_length):
    eq_lines = print("=" * req_length)
    return eq_lines